<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zxq.mapper.UserMapper">
    <update id="updateByUser">
        update user set
        <if test="password!=null">
            password=#{password}
        </if>
        <if test="sex!=null">
            sex=#{sex}
        </if>
        where id=#{id}
    </update>


    <select id="getAllUsers" resultType="com.zxq.domain.User">
          select * from user
    </select>

    <select id="selectById" resultType="com.zxq.domain.User">
        select * from user where id=#{id}
    </select>
</mapper>